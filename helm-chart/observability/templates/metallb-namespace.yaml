{{- if .Values.metallb.enabled }}
apiVersion: v1
kind: Namespace
metadata:
  name: {{ .Values.metallb.namespace }}
  labels:
    name: {{ .Values.metallb.namespace }}
    app.kubernetes.io/name: metallb
{{- end }}
